# 專題規範：題目、要求與評分

> 資料庫管理・**每人獨立完成一個「實際會動的資料庫應用程式」**・15 分鐘簡報（含現場 demo）
> 題目與其完整規格（情境、資料表建議、必備功能、進階選項、報表點子、demo 亮點）說明將於 **U03 課後**公布指派；本文件是**共同要求**的唯一權威規格。

## 0. 設計理念

- 專題＝實做**可用的多人資訊系統**：具備正規化 schema、約束把關、交易保護、報表分析、圖形介面。
- 課堂上會示範資料庫「內部」怎麼運作（儲存、索引、查詢引擎、交易——見講義 U06–U09）；專題則是把資料庫「用好」。
- **AI 時代的規則**：歡迎用 AI（ChatGPT / Claude / Gemini）協助設計與寫程式（vibe coding），但
  1. **要能解釋交出的每一行程式**。報告時教師任指一段要求說明；說不出來的部分視同沒做。
  2. notebook 末尾必須有「AI 使用說明」一節（見 §2 第 9 點）。
  3. 兩人交出雷同程式且都無法各自解釋時，以抄襲處理。

## 1. 統一技術棧（全班一致，不接受其他選擇）

| 項目 | 規定 | 理由 |
|---|---|---|
| 開發與展示平台 | **Google Colab**（一個 `.ipynb` 檔案完成整個專題） | 具備瀏覽器與網路連線就能跑 |
| 資料庫 | **SQLite**（Python 內建 `sqlite3`） | 零安裝、單檔、課堂主力 |
| GUI | **Gradio**（`import gradio as gr`；Colab 直接內嵌） | 簡單、AI 產碼品質高、`share=True` 一鍵得到臨時網址，報告時可讓同學手機操作 |
| 資料處理與圖表 | pandas ＋ matplotlib（圖表以 `gr.Plot` 嵌入介面） | 統計系本行 |
| 資料合成 | numpy（**固定 random seed**，重跑可重現） | 沒有真使用者，就自己造出擬真流量 |

> Colab 上安裝：`!pip -q install gradio`（每次開新 runtime 跑一次，約 30 秒）。
> 教師的兩個**示範專題（圖書館借閱管理、個人記帳分析——皆不在指派清單內）**有完整影片與程式：請模仿它的**結構與完成度**，而不是抄它的程式。

## 2. 共同要求

1. **Schema**：≥ 4 個資料表、3NF、主鍵／外鍵齊全、另外用上 ≥ 3 種約束（`NOT NULL` / `UNIQUE` / `CHECK` / `DEFAULT`）；`PRAGMA foreign_keys = ON`；附一張 schema 圖（手繪拍照、dbdiagram.io 匯出皆可）。
2. **合成資料**：程式生成 ≥ **10,000 列**交易級資料（訂單、借閱、報名⋯⋯），固定 seed；分佈要「像真的」（長尾熱門度、日夜／季節效應⋯⋯統計系請展現品味）。
3. **CRUD**：主要實體的新增／查詢／修改／刪除齊全；查詢一律用 `?` 佔位符傳值（這是 sqlite3 傳參數的正常寫法，也讓含引號、日期的值不出錯）；表單驗證與友善錯誤訊息（約束違反時使用者看得懂）。
4. **交易與競態**：至少一個「必須原子完成」的業務操作（扣名額、借出、成交、轉帳⋯⋯）以交易包好並示範 rollback；並示範一個**併發競態**（如兩人同搶最後一個名額：先用兩條 connection 重現超賣，再展示你的防護——條件式 UPDATE 或 `BEGIN IMMEDIATE`）。
5. **報表 ≥ 5 張**：其中 ≥ 1 個 window function、≥ 2 個多表 join、≥ 1 張圖表（matplotlib 嵌入 `gr.Plot`）；每張報表附一句「這張表回答什麼問題」。
6. **效能**：挑最慢的一個查詢做 `EXPLAIN QUERY PLAN`，建索引前後計時對照（資料量 10,000+ 列才看得出差異，這就是第 2 點的用途之一）。
7. **Gradio UI**：≥ 3 個分頁（日常操作／後台管理／統計報表），查詢結果用 `gr.Dataframe` 呈現；介面所有功能都要接上真實資料庫。
8. **測試 ≥ 8 個 assert**：直接呼叫你的處理函數（不經 UI），含至少 2 個「應該失敗」的情境（約束違反、超賣被擋）。
9. **AI 使用說明**（notebook 末節）：用了哪些工具、關鍵 prompt 摘錄、AI 哪裡寫錯／如何發現與修正、心得 3–5 行；未使用 AI 就寫「未使用」。
10. **簡報 15 分鐘**（12 分鐘內容 ＋ 2 分鐘 Q&A ＋ 1 分鐘換場，時間到即切）：動機與情境 1–2 分 → schema 與設計決策 2–3 分 → **live demo 5–6 分**（走預先寫好的腳本路徑，勿即興）→ 報表解讀 2 分 → 困難與 AI 協作心得 1 分。**demo 必備備援**（截圖／GIF／30 秒預錄）。

**繳交**：報告當天現場以自己的 Colab 開啟報告；**報告結束後**繳交最終版 `.ipynb`（方式課堂公布）。

## 3. 評分面向

| 面向 | 看什麼 |
|---|---|
| 功能完成度與正確性 | §2 共同要求 1–4、7–8 是否全數做到且正確；測試可信 |
| Schema 設計與資料品質 | 正規化是否到位、約束是否真的把關、合成資料像不像真的 |
| 報表與分析深度 | 查詢的統計含金量、圖表與解讀（統計系請拿出專業） |
| 簡報與 demo | 結構、時間控制、demo 流暢與備援 |
| Q&A 與程式理解 | 能否解釋設計決策與任指的程式片段 |
